.worktrees/
node_modules/
dist/
.astro/
.wrangler/
.dev.vars*
.env*
!.env.example
.source-work/
.artifacts/
.change-state/
# Ingestion raw inputs and candidate builds are operational artifacts.  `changes/`
# remains deliberately trackable as the sanitized publication record.
.artifacts/intake/
.artifacts/candidates/
test-results/
playwright-report/
**/.source-copy-*.tmp
